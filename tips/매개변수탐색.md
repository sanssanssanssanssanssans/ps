# 매개변수 탐색

- 먼저 매개변수 탐색이 가능한지 확인하자. 어떤 값 $x$에 대해, 가능하면 그보다 큰/작은 값도 항상 가능해야한다.

## 최소 / 최대

최소값을 찾는거면 lower_bound, 최댓값을 찾는거면 upper_bound를 기준으로 mid를 처리하자. 최댓값은, $ (l + r + 1) \div 2 $고, 최소값이면, $(l + r) \div 2$이다.